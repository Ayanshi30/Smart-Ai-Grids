"use client"

import { useState } from "react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { 
  AlertTriangle, Bell, Check, Clock, Filter, 
  XCircle, AlertCircle, Info, Zap
} from "lucide-react"

const alerts = [
  {
    id: 1,
    type: "critical",
    title: "Transformer T-142 Overheating",
    description: "Temperature exceeded 94°C threshold. Immediate attention required.",
    timestamp: "2 minutes ago",
    location: "Central Delhi Substation",
    acknowledged: false
  },
  {
    id: 2,
    type: "critical",
    title: "High Load Alert - Zone Central",
    description: "Load reached 95% capacity. Risk of power outage if not addressed.",
    timestamp: "15 minutes ago",
    location: "Central Delhi",
    acknowledged: false
  },
  {
    id: 3,
    type: "warning",
    title: "Frequency Deviation Detected",
    description: "Grid frequency dropped to 49.85 Hz. Auto-correction in progress.",
    timestamp: "28 minutes ago",
    location: "North Delhi Grid",
    acknowledged: false
  },
  {
    id: 4,
    type: "warning",
    title: "Solar Farm Output Fluctuation",
    description: "SF-01 experiencing 15% output variance due to cloud cover.",
    timestamp: "45 minutes ago",
    location: "Gurgaon Solar Farm",
    acknowledged: true
  },
  {
    id: 5,
    type: "info",
    title: "Scheduled Maintenance Reminder",
    description: "Wind Turbine WT-08 maintenance scheduled for tomorrow 6 AM.",
    timestamp: "1 hour ago",
    location: "Faridabad",
    acknowledged: true
  },
  {
    id: 6,
    type: "info",
    title: "Peak Hour Approaching",
    description: "System entering peak demand period (4 PM - 8 PM). All systems nominal.",
    timestamp: "2 hours ago",
    location: "System-wide",
    acknowledged: true
  },
  {
    id: 7,
    type: "warning",
    title: "Battery Storage Low",
    description: "BS-01 charge level at 25%. Consider reducing discharge rate.",
    timestamp: "3 hours ago",
    location: "West Delhi Storage",
    acknowledged: true
  },
  {
    id: 8,
    type: "info",
    title: "Renewable Generation Record",
    description: "Solar generation exceeded daily target by 12%. New record achieved.",
    timestamp: "5 hours ago",
    location: "All Solar Farms",
    acknowledged: true
  },
]

const getAlertIcon = (type: string) => {
  switch (type) {
    case "critical": return XCircle
    case "warning": return AlertTriangle
    default: return Info
  }
}

const getAlertStyles = (type: string) => {
  switch (type) {
    case "critical": 
      return "bg-red-500/10 border-red-500/30 text-red-400"
    case "warning": 
      return "bg-yellow-500/10 border-yellow-500/30 text-yellow-400"
    default: 
      return "bg-[#00d4ff]/10 border-[#00d4ff]/30 text-[#00d4ff]"
  }
}

export default function AlertsPage() {
  const [filter, setFilter] = useState<"all" | "critical" | "warning" | "info">("all")
  const [alertList, setAlertList] = useState(alerts)

  const filteredAlerts = filter === "all" 
    ? alertList 
    : alertList.filter(a => a.type === filter)

  const acknowledgeAlert = (id: number) => {
    setAlertList(prev => prev.map(a => 
      a.id === id ? { ...a, acknowledged: true } : a
    ))
  }

  const criticalCount = alertList.filter(a => a.type === "critical" && !a.acknowledged).length
  const warningCount = alertList.filter(a => a.type === "warning" && !a.acknowledged).length
  const infoCount = alertList.filter(a => a.type === "info" && !a.acknowledged).length

  return (
    <div className="space-y-6">
      {/* Header */}
      <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
        <div>
          <h1 className="text-2xl font-bold text-foreground">Alerts Center</h1>
          <p className="text-muted-foreground">Monitor and manage system notifications</p>
        </div>
        <Button className="bg-[#00ff88] text-black hover:bg-[#00ff88]/80">
          <Bell className="w-4 h-4 mr-2" />
          Configure Alerts
        </Button>
      </div>

      {/* Alert Summary */}
      <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <Card className="bg-red-500/10 border-red-500/30">
          <CardContent className="pt-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm text-muted-foreground">Critical</p>
                <p className="text-3xl font-bold text-red-400">{criticalCount}</p>
              </div>
              <XCircle className="w-10 h-10 text-red-400" />
            </div>
          </CardContent>
        </Card>
        <Card className="bg-yellow-500/10 border-yellow-500/30">
          <CardContent className="pt-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm text-muted-foreground">Warnings</p>
                <p className="text-3xl font-bold text-yellow-400">{warningCount}</p>
              </div>
              <AlertTriangle className="w-10 h-10 text-yellow-400" />
            </div>
          </CardContent>
        </Card>
        <Card className="bg-[#00d4ff]/10 border-[#00d4ff]/30">
          <CardContent className="pt-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm text-muted-foreground">Informational</p>
                <p className="text-3xl font-bold text-[#00d4ff]">{infoCount}</p>
              </div>
              <Info className="w-10 h-10 text-[#00d4ff]" />
            </div>
          </CardContent>
        </Card>
      </div>

      {/* Filters */}
      <div className="flex items-center gap-2 flex-wrap">
        <Filter className="w-4 h-4 text-muted-foreground" />
        <Button 
          size="sm" 
          variant={filter === "all" ? "default" : "outline"}
          onClick={() => setFilter("all")}
          className={filter === "all" ? "bg-[#00ff88] text-black hover:bg-[#00ff88]/80" : "border-border text-foreground hover:bg-muted"}
        >
          All
        </Button>
        <Button 
          size="sm" 
          variant={filter === "critical" ? "default" : "outline"}
          onClick={() => setFilter("critical")}
          className={filter === "critical" ? "bg-red-500 text-white hover:bg-red-600" : "border-border text-foreground hover:bg-muted"}
        >
          Critical
        </Button>
        <Button 
          size="sm" 
          variant={filter === "warning" ? "default" : "outline"}
          onClick={() => setFilter("warning")}
          className={filter === "warning" ? "bg-yellow-500 text-black hover:bg-yellow-600" : "border-border text-foreground hover:bg-muted"}
        >
          Warnings
        </Button>
        <Button 
          size="sm" 
          variant={filter === "info" ? "default" : "outline"}
          onClick={() => setFilter("info")}
          className={filter === "info" ? "bg-[#00d4ff] text-black hover:bg-[#00d4ff]/80" : "border-border text-foreground hover:bg-muted"}
        >
          Info
        </Button>
      </div>

      {/* Alerts List */}
      <Card className="bg-card border-border">
        <CardHeader>
          <CardTitle className="text-foreground flex items-center gap-2">
            <AlertCircle className="w-5 h-5 text-[#00ff88]" />
            Recent Alerts ({filteredAlerts.length})
          </CardTitle>
        </CardHeader>
        <CardContent>
          <div className="space-y-3">
            {filteredAlerts.map((alert) => {
              const Icon = getAlertIcon(alert.type)
              return (
                <div 
                  key={alert.id}
                  className={`p-4 rounded-lg border transition-all ${
                    alert.acknowledged ? "opacity-60" : ""
                  } ${getAlertStyles(alert.type)}`}
                >
                  <div className="flex items-start justify-between gap-4">
                    <div className="flex items-start gap-3">
                      <Icon className="w-5 h-5 mt-0.5 flex-shrink-0" />
                      <div>
                        <h4 className="font-semibold text-foreground">{alert.title}</h4>
                        <p className="text-sm text-muted-foreground mt-1">{alert.description}</p>
                        <div className="flex items-center gap-4 mt-2 text-xs text-muted-foreground">
                          <span className="flex items-center gap-1">
                            <Clock className="w-3 h-3" />
                            {alert.timestamp}
                          </span>
                          <span className="flex items-center gap-1">
                            <Zap className="w-3 h-3" />
                            {alert.location}
                          </span>
                        </div>
                      </div>
                    </div>
                    {!alert.acknowledged && (
                      <Button 
                        size="sm" 
                        variant="ghost"
                        onClick={() => acknowledgeAlert(alert.id)}
                        className="text-foreground hover:bg-muted shrink-0"
                      >
                        <Check className="w-4 h-4 mr-1" />
                        Acknowledge
                      </Button>
                    )}
                    {alert.acknowledged && (
                      <span className="text-xs text-muted-foreground flex items-center gap-1">
                        <Check className="w-3 h-3" />
                        Acknowledged
                      </span>
                    )}
                  </div>
                </div>
              )
            })}
          </div>
        </CardContent>
      </Card>
    </div>
  )
}